rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default read access for public portfolio items and authors
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null ; // TODO: add uid check || request.auth.uid == resource.data.ownerId;
    }
  }
}

